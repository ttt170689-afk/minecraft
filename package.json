{
  "name": "browsercraft",
  "version": "2.0.0",
  "private": true,
  "description": "BrowserCraft mega content pack — Minecraft-like browser game",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "build": "npm install --omit=dev",
    "server": "node server/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
